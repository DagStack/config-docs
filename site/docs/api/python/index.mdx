---
sidebar_position: 1
title: Python API — overview
description: Navigation for the public API reference of the dagstack-config package; pages are generated by pydoc-markdown from the source.
slug: /api/python
---

# Python API — overview

The reference is generated automatically from the `dagstack-config` package source through [pydoc-markdown](https://niklasrosenstein.github.io/pydoc-markdown/).

:::info Generation deferred
The pages have not been generated for this initial release. The pipeline configuration lives in `pydoc-markdown.yaml` at the repository root; run it with `make api-python`. Content will be filled in by subsequent commits, once the implementation has stabilized.
:::

## Reference contents (planned)

| Module | What it covers |
|---|---|
| `dagstack.config` | Public top level — `Config`, `load`, `load_from`, `load_paths`, accessors (snake_case), errors. |
| `dagstack.config.sources` | `ConfigSource`, `YamlFileSource`, `JsonFileSource`, `InMemorySource`. |
| `dagstack.config.errors` | `ConfigError`, `ConfigErrorReason`. |
| `dagstack.config.subscription` | `Subscription` (Phase 1 — inactive, the callback is never invoked). |

## Generating it locally

```bash
pip install pydoc-markdown
pip install dagstack-config      # into the active environment
cd <repo-root>
make api-python
```

## Until the reference is generated

- Docstrings are accessible through `help(dagstack.config)` in the interpreter.
- Guide pages: [Declare a section](/docs/guides/declaring-section), [Testing](/docs/guides/testing).
- Specification: [ADR-0001](/docs/spec/adr/0001-yaml-configuration).
