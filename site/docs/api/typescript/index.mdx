---
sidebar_position: 1
title: TypeScript API — overview
description: Navigation for the public API reference of the @dagstack/config package; pages are generated by TypeDoc from the source.
slug: /api/typescript
---

# TypeScript API — overview

The reference is generated automatically from the `@dagstack/config` npm package source through [TypeDoc](https://typedoc.org/) plus the markdown plugin.

:::info Generation deferred
The pages have not been generated for this initial release. Run them with
`make api-typescript` (the target will land in the `Makefile` once the
TypeDoc pipeline is integrated). Content will be filled in by subsequent
commits, once the implementation has stabilized.
:::

## Reference contents (planned)

| Module | What it covers |
|---|---|
| `@dagstack/config` | Public top level — `Config`, `load`, `loadFrom`, accessors, errors. |
| `@dagstack/config` — sources | The `ConfigSource` interface, `YamlFileSource`, `JsonFileSource`, `InMemorySource`. |
| `@dagstack/config` — errors | `ConfigError`, `ConfigErrorReason`. |

## Generating it locally

```bash
cd config-typescript
git submodule update --init
npm install
npm run docs      # once the TypeDoc pipeline is integrated
```

## Until the reference is generated

- JSDoc from the source is visible in your IDE (VSCode / WebStorm / Neovim with LSP).
- `.d.ts` types are available after `npm install @dagstack/config`.
- Guide pages: [Declare a section](/docs/guides/declaring-section), [Testing](/docs/guides/testing).
- Specification: [ADR-0001](/docs/spec/adr/0001-yaml-configuration).
